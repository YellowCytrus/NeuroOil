# Деплой на GitHub Pages

Этот проект можно задеплоить на GitHub Pages, но есть важные ограничения:

## ⚠️ Важные замечания

1. **GitHub Pages хостит только статические сайты** - это означает, что только **фронтенд** можно задеплоить на GitHub Pages
2. **Бэкенд нужно задеплоить отдельно** на другой платформе (Render, Railway, Heroku, Vercel и т.д.)

## 📋 Шаги для деплоя

### 1. Подготовка репозитория

1. Убедитесь, что ваш код находится в GitHub репозитории
2. Если репозиторий называется не `neuro-oil`, обновите `base` в `frontend/vite.config.ts`:
   ```typescript
   base: process.env.GITHUB_PAGES ? '/ваше-имя-репозитория/' : '/',
   ```

### 2. Настройка GitHub Pages

1. Перейдите в Settings → Pages вашего репозитория
2. В разделе "Source" выберите "GitHub Actions"
3. GitHub Actions workflow автоматически задеплоит фронтенд при каждом push в main/master

### 3. Деплой бэкенда

Вам нужно задеплоить бэкенд на одну из платформ:

#### Вариант A: Render (рекомендуется)

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Build Command**: `cd backend && pip install -r requirements.txt`
   - **Start Command**: `cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT`
   - **Environment**: Python 3
5. После деплоя скопируйте URL (например: `https://your-app.onrender.com`)

#### Вариант B: Railway

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Настройте переменные окружения и команду запуска
4. Railway автоматически определит Python проект

#### Вариант C: Heroku

1. Установите Heroku CLI
2. Создайте `Procfile` в корне проекта:
   ```
   web: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```
3. Деплой через Heroku CLI

### 4. Настройка переменных окружения

После деплоя бэкенда:

1. Получите URL вашего бэкенда (например: `https://your-backend.onrender.com`)
2. В GitHub репозитории перейдите в Settings → Secrets and variables → Actions
3. Добавьте новый секрет:
   - **Name**: `VITE_API_URL`
   - **Value**: `https://your-backend.onrender.com/api` (замените на ваш URL)

### 5. Обновление CORS в бэкенде

Убедитесь, что в `backend/app/main.py` CORS настроен правильно:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-username.github.io",  # Ваш GitHub Pages URL
        "http://localhost:3000",  # Для локальной разработки
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 6. Локальная проверка перед деплоем

Для проверки локально:

```bash
cd frontend
GITHUB_PAGES=true VITE_API_URL=https://your-backend-url.com/api npm run build
npm run preview
```

## 🔧 Ручной деплой (без GitHub Actions)

Если вы хотите задеплоить вручную:

1. Соберите фронтенд:
   ```bash
   cd frontend
   GITHUB_PAGES=true VITE_API_URL=https://your-backend-url.com/api npm run build
   ```

2. Переключитесь на ветку `gh-pages`:
   ```bash
   git checkout -b gh-pages
   ```

3. Скопируйте содержимое `frontend/dist` в корень репозитория

4. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Deploy to GitHub Pages"
   git push origin gh-pages
   ```

5. В Settings → Pages выберите ветку `gh-pages` как источник

## 📝 Примечания

- GitHub Pages бесплатен для публичных репозиториев
- Деплой через GitHub Actions происходит автоматически при каждом push
- Убедитесь, что бэкенд доступен публично (не требует аутентификации)
- Для production рекомендуется использовать HTTPS для бэкенда

## 🐛 Решение проблем

### Фронтенд не загружается
- Проверьте `base` в `vite.config.ts` - он должен соответствовать имени репозитория
- Убедитесь, что GitHub Actions workflow выполнился успешно

### API запросы не работают
- Проверьте переменную `VITE_API_URL` в GitHub Secrets
- Убедитесь, что бэкенд доступен и CORS настроен правильно
- Проверьте консоль браузера на наличие ошибок CORS

### Бэкенд не отвечает
- Проверьте логи бэкенда на платформе деплоя
- Убедитесь, что все зависимости установлены
- Проверьте, что порт настроен правильно (используйте переменную окружения `$PORT`)

